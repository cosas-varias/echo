<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Basic functionality -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- GPS logging. Location is only ever read while the foreground service is running, which
         is what lets Echo do without ACCESS_BACKGROUND_LOCATION. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Optional shake-triggered camera capture. Used only from the foreground service. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- A short tick confirms a clip has started recording, since nothing else can. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Optional screenshots via MediaProjection, granted by the user each session. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Sending traces to a server and deleting them once they land. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/Theme" >
        <activity
            android:name=".SaidItActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SaidItService"
            android:foregroundServiceType="microphone|location|camera|mediaProjection"
            android:enabled="true"
            android:exported="false">
        </service>

        <activity
            android:name=".DiagnosticsActivity"
            android:parentActivityName=".SaidItActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="SaidItActivity" />
        </activity>

        <activity
            android:name=".TracesActivity"
            android:parentActivityName=".SaidItActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="SaidItActivity" />
        </activity>

        <activity
            android:name=".SurveyActivity"
            android:parentActivityName=".SaidItActivity"
            android:windowSoftInputMode="adjustResize" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="SaidItActivity" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/path_provider">

            </meta-data>
        </provider>
    </application>

</manifest>
